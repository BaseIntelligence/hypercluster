"""External QA helpers for Hypercluster mission ops.

These modules are intentionally outside ``src/hypercluster`` so they never
become product dependencies. Verda (and similar cloud control planes) are
ops-only rentals used to prove home-grown marketplace registration works
against a real single-GPU host (M8 / VAL-LIVE-*).
"""
